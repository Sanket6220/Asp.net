@model IEnumerable<Employee>

@{
    ViewData["Title"] = "GetEmployeeByJob";
}

<from asp-action="GetEmployeeByJob">
    <input name="Job"/>
    <input type="submit" value="Search"/>
</from>

@{
    if (ViewBag.RequestType == "POST")
    {
    <table>
                    <tr>
                        <th>EmployeeName</th>
                        <th>Job</th>
                        <th>Salary</th>
                        <th>Deptno</th>
                    </tr>

                    @foreach(var item in Model)
            {
                <tr>
                    <td>@item.EmployeeName</td>
                    <td>@item.Job</td>
                    <td>@item.Salary</td>
                    <td>@item.Deptno</td>
                    
                </tr>
            }
        </table>
    }
}